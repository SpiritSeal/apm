[package]
name = "apm"
version = "0.1.0"
edition = "2021"

[dependencies]
rdev = { version = "0.5.3", features=["unstable_grab"] }

[features]
default = ["unstable_grab"]
unstable_grab = ["rdev/unstable_grab"]

